// import FireAnalytics from 'react-native-firebase-analytics';

class Analytics {
  static setScreenName(name) {
    // FireAnalytics.setScreenName(name);
  }

  static logEvent(name, parameters) {
    // FireAnalytics.logEvent(name, parameters);
  }

  static setUserId(userId) {
    // FireAnalytics.setUserId(userId);
  }

  static setUserProperty(name, property) {
    // FireAnalytics.setUserProperty(name, property);
  }
}

export default Analytics;
